# 数据类型转换



```go
package main

import "fmt"

type Stu struct {
	Name string
}
var stu Stu
var i int
var i8 int8
var ui8 uint8
var b byte
var boolean bool
var f float64
var str string
var fu func(int) int
var ba [5]int
var bs []int
var bb []bool
var bba [5]bool
var mapzero map[string]int
var interfaceZero interface{}
var chanZero chan int
var pointZero *int

func main() {
	fmt.Println("strucrt zero:", stu)
	fmt.Println("int zero:", i)
	fmt.Println("int8 zero:", i8)
	fmt.Println("uint8 zero:", ui8)
	fmt.Println("byte zero:", b)
	fmt.Println("bool zero:", boolean)
	fmt.Println("float64 zero:", f)
	fmt.Println("string zero:", str)
	fmt.Println("func zero:", fu)
	fmt.Println("[5]int zero:", ba)
	fmt.Println("[]int zero:", bs)
	fmt.Println("[]bool zero:", bb)
	fmt.Println("[5]bool zero:", bba)
	fmt.Println("[5]bool zero len:", len(bba))
	fmt.Println("[5]bool zero cap:", cap(bba))
	fmt.Println("[]bool zero len:", len(bb))
	fmt.Println("[]bool zero cap:", cap(bb))
	fmt.Println("mapzero zero:", mapzero)
	fmt.Println("mapzero == nil ?", mapzero == nil)
	fmt.Println("interface zero:", interfaceZero)
	fmt.Println("chan zero:", chanZero)
	fmt.Println("point zero:", pointZero)
}
```

结果:

```go
strucrt zero: {}
int zero: 0
int8 zero: 0
uint8 zero: 0
byte zero: 0
bool zero: false
float64 zero: 0
string zero: 
func zero: <nil>
[5]int zero: [0 0 0 0 0]
[]int zero: []
[]bool zero: []
[5]bool zero: [false false false false false]
[5]bool zero len: 5
[5]bool zero cap: 5
[]bool zero len: 0
[]bool zero cap: 0
mapzero zero: map[]
mapzero == nil ? true
interface zero: <nil>
chan zero: <nil>
point zero: <nil>

```

你只需要知道,nil能赋值给哪些类型的变量,那么就可以和哪些变量做比较.

## 类型的隐式转换

对于字面的值,编译器会做一些转换,例如:

```go
	var myInt int32     = 5
	var myFloat float64 = 0
```

##接口的转换遵循以下规则：

1. 普通类型向接口类型转换是隐式的
2. 接口类型向普通类型转换需要类型断言

```go
package main

import (
	"fmt"
)

func main() {
	var val interface{} = "hello"
	fmt.Println(val)
	val = []byte{'a', 'b', 'c'}
	fmt.Println(val)
}
```

"hello"作为string类型存储在interface{}类型的变量val中，[]byte{'a', 'b', 'c'}作为slice存储在interface{}类型的变量val中。这个过程是隐式的，是编译期确定的

## 接口的具体实现

```go
package main

import (
	"fmt"
)

// 演讲者接口
type Speaker interface {
	// 说
	Say(string)
	// 听
	Listen(string) string
	// 打断、插嘴
	Interrupt(string)
}

// 王兰讲师
type WangLan struct {
	msg string
}

func (this *WangLan) Say(msg string) {
	fmt.Printf("王兰说：%s\n", msg)
}

func (this *WangLan) Listen(msg string) string {
	this.msg = msg
	return msg
}

func (this *WangLan) Interrupt(msg string) {
	this.Say(msg)
}

// 江娄讲师
type JiangLou struct {
	msg string
}

func (this *JiangLou) Say(msg string) {
	fmt.Printf("江娄说：%s\n", msg)
}

func (this *JiangLou) Listen(msg string) string {
	this.msg = msg
	return msg
}

func (this *JiangLou) Interrupt(msg string) {
	this.Say(msg)
}

func main() {
	wl := &WangLan{}
	jl := &JiangLou{}

	//person是Speaker的具体实现,可以理解为person是Speaker类型的变量
	//var i int && i = 2
	var person Speaker
	person = wl
	person.Say("Hello World!")
	person = jl
	person.Say("Good Luck!")
}
```

三种断言方式:

1. 带ok的类型断言

   ```go
   if value, ok := element.(string); ok {
   			fmt.Printf("html[%d] is a string and its value is %s\n", index, value)
   		} else if value, ok := element.([]byte); ok {
   			fmt.Printf("html[%d] is a []byte and its value is %s\n", index, string(value))
   		}
   ```

2. 不带ok的类型断言

   ```go
   value := element.(T)
   ```

3. switch

   ```go
   switch value := element.(type) {
   		case string:
   			fmt.Printf("html[%d] is a string and its value is %s\n", index, value)
   		case []byte:
   			fmt.Printf("html[%d] is a []byte and its value is %s\n", index, string(value))
   		case int:
   			fmt.Printf("invalid type\n")
   		default:
   			fmt.Printf("unknown type\n")
   		}
   ```

   