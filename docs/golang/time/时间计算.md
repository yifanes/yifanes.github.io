# 时间计算相关的片段代码

## 将字面量的数字转换为Duration

1.通过time.ParseDuration()

> ```go
> time.ParseDuration("1h1m1s")
> ```

2.如果是数字直接转

> ```go
> time.Duration(-120) * time.Second
> ```



## 时间相加减

1.time.Now().Add()

> Add方法接受Duration参数,返回`time`类型

2.time.Sub()

> 接受time类型的参数,返回Duration类型的结果



End.